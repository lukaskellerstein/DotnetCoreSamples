namespace App
{


    static class MyLambda
    {


        public static void Run()
        {


            // 1. Usage in Delegates
            // ex. Func<int, string, bool> isTooLong = (int x, string s) => s.Length > x;

            // 2. Usage in LINQ
            // ex. customers.Where(c => c.City == "London");

        }


    }
}